# 解决 DSH 长程任务中的 context overflow 误报问题

## bug 现象

最近我在使用 DSH 跑长程任务时，遇到了一个很奇怪的问题：

1. DSH 报错：

```text
pi-ai detected context overflow for model "glm-5.3-flash"
```

2. 但是接着对话，会话还是可以进行下去；
3. Agent 仍然可以调用工具、读取文件和修改代码；
4. 但是最终的自然语言回答经常失败；
5. `/compact` 命令也会失败。

一开始我以为，应该是会话真的超过模型上下文窗口了。

但是如果真的超过了上下文窗口，后面的请求应该也无法继续才对。现在的情况却是：工具还能正常调用，只是最终回答容易失败。

因此，我开始怀疑这并不是一次真正的 context overflow。

## bug 原因

经过排查，我发现问题出在 `llm-pi-ai` 的模型配置上。

如果一个自定义模型没有声明 `contextWindow`，DSH 会使用下面这个默认值：

```text
262144
```

也就是 262K tokens。

但是，262K 只是 DSH/pi-ai 在不知道模型真实上下文窗口时使用的默认值，并不一定是模型服务端真正支持的上限。

在我的实际会话中，GLM 服务端已经成功处理了超过 50 万，甚至超过 70 万 tokens 的请求。

也就是说，模型服务端其实没有发生上下文溢出。

问题在于，pi-ai 在收到最终响应后，会检查：

```text
输入 token + cache read token > contextWindow
```

由于 DSH 记录的 `contextWindow` 仍然是 262144，所以只要请求超过 262K，pi-ai 就可能把服务端已经成功返回的响应重新判断为 context overflow。

整个过程大概是：

1. DSH 向 GLM 发送一个超过 262K tokens 的请求；
2. GLM 服务端成功处理请求；
3. GLM 返回正常结果；
4. pi-ai 发现请求用量超过了配置中的 262K；
5. pi-ai 将这个成功响应重新判断为 context overflow；
6. DSH 最终显示错误。

因此，这次错误并不能证明模型真的超过了上下文窗口。

## 为什么对话还可以继续

这也解释了为什么报错之后，对话仍然可以继续。

工具调用结束时，一般会返回 `toolUse`。

最终自然语言回答结束时，一般会返回 `stop`。

pi-ai 的这条上下文溢出判断主要影响最终的 `stop` 响应。因此可能出现：

1. Agent 正常读取文件；
2. Agent 正常调用工具；
3. Agent 正常修改代码；
4. 最后准备回答时，被判断为 context overflow。

所以看起来就像 Agent 还能继续工作，但是没办法正常输出最后的回答。

## 为什么 `/compact` 也会失败

`/compact` 会使用当前模型对历史会话进行总结。

但是，用来生成摘要的模型仍然使用错误的 262K 上下文窗口。

因此可能出现：

1. DSH 把长会话发送给 GLM 生成摘要；
2. GLM 成功生成摘要；
3. pi-ai 发现请求超过配置中的 262K；
4. 摘要响应被重新判断为 context overflow；
5. 压缩结果没有真正写入会话。

由于历史记录没有缩短，下一次执行 `/compact` 时，还是会重复同样的问题。

## 如何解决

解决方法其实很简单：

> 为模型配置正确的 `contextWindow`。

例如，我使用的 GLM Coding Plan 路由实际支持接近 1M tokens 的上下文。

将它的 `contextWindow` 从默认的：

```text
262144
```

修改为：

```text
1000000
```

之后，之前的 context overflow 误报就消失了，`/compact` 也可以正常工作。

但是，手动修改配置比较麻烦。

而且同一个模型可能通过不同 provider 或网关使用，例如：

```text
glm-coding/glm-5.3-flash
```

和：

```text
zai-coding-cn/glm-5.3-flash
```

虽然它们的模型 ID 相同，但是实际部署和上下文窗口不一定相同。

因此，我写了一个 DSH 插件：

```text
dsh-model-context-catalog
```

## 插件做了什么

这个插件会为已经配置的 `llm-pi-ai` 模型维护一份上下文窗口配置。

它主要完成三件事：

1. 读取 DSH 中已经配置的 provider 和模型；
2. 为精确的 `provider/model` 路由保存 `contextWindow`；
3. 通过 DSH 的 Settings API，将正确的数值同步到 `llm-pi-ai`。

插件不会修改 DSH 或 pi-ai 的内部代码，也不会读取或者接管 provider 的 API Key。

它只负责修正模型的上下文窗口信息。

## 使用效果

安装插件后，可以在 DSH 设置页面中直接管理模型的上下文窗口：

![上下文窗口设置页面](https://raw.githubusercontent.com/HOWILLMAKEIT/dsh-model-context-catalog/main/image.png)

模型选择器会显示：

1. provider 名称；
2. provider key；
3. 模型名称；
4. 模型 ID；
5. `(Agent Plan)` 等原始后缀。

这些信息不会被修改或者隐藏，方便区分不同 provider 下的模型。

## 安装方式

插件已经发布到 npm。

运行：

```bash
dsh plugin --profile web add dsh-model-context-catalog
```

如果需要从本地目录安装：

```bash
dsh plugin --profile web add /absolute/path/to/dsh-model-context-catalog
```

安装完成后，刷新 DSH Web 页面。

如果设置页面中没有出现插件，可以重启一次 DSH Host。

## 使用方法

打开：

```text
设置 → 上下文窗口
```

然后：

1. 点击“添加模型”；
2. 选择已经配置的模型；
3. 输入该模型真实支持的上下文窗口；
4. 根据需要填写备注；
5. 点击保存；
6. 等待状态变为“已生效”。

例如，如果当前模型部署明确支持 1M tokens，可以填写：

```text
1000000
```

## 注意事项

上下文窗口需要根据实际使用的 provider、套餐和模型部署填写。

不要直接认为所有同名模型都支持相同的上下文窗口。

如果填写得太小，仍然可能出现 context overflow 误报。

如果填写得太大，则可能导致真正的上下文溢出无法被及时发现。

## 项目地址

GitHub：

https://github.com/HOWILLMAKEIT/dsh-model-context-catalog

npm：

https://www.npmjs.com/package/dsh-model-context-catalog

如果你在使用 DSH 跑长程任务时，也遇到了相同的问题，可以尝试使用这个插件。
